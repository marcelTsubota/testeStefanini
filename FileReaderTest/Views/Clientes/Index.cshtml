@model IEnumerable<FileReaderTest.Models.Cliente>
@using (Html.BeginForm("Submit", "Clientes", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    {
        ViewBag.Title = "Index";
    }

<h2>Index</h2>

<p>
    @*@Html.ActionLink("Arquivo", "ReadFile", "Clientes", Request.FilePath)*@
    <input type="file" name="file" />
    <input type="submit" value="Ler" />
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sobrenome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataNasc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sexo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>
    <tr>
        <td>
            @{ var data = ViewBag.Message; }
            @data
        </td>
    </tr>

        @*@foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Sobrenome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DataNasc)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Sexo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Status)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id=item.ClienteID }) |
                        @Html.ActionLink("Details", "Details", new { id=item.ClienteID }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.ClienteID })
                    </td>
                </tr>
            }*@

</table>
}